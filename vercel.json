{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/server.js" }
  ],
  "functions": {
    "server.js": {
      "maxDuration": 60
    }
  }
}
