# Dependencies
node_modules/

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local

# Build output
dist/

.vercel

# Local database — contains PII (pastor names, phones, emails)
churches-db.json
